import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  boxContainer: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
    marginTop: 50,
  },
});
